import streamlit as st
import google.generativeai as genai
from PIL import Image

# --- إعداد الصفحة ---
st.set_page_config(
    page_title="مدقق رخص البناء - أمانة الرياض",
    page_icon="🏗️",
    layout="wide",
    initial_sidebar_state="expanded"
)

# --- الواجهة الجانبية لإدخال المفاتيح ---
with st.sidebar:
    st.image("https://cdn-icons-png.flaticon.com/512/2942/2942544.png", width=100)
    st.title("إعدادات النظام")
    api_key = st.text_input("أدخل مفتاح Gemini API هنا", type="password")
    st.info("هذا النظام يعمل بالذكاء الاصطناعي لمساعدة المهندس المعماري في تدقيق المخططات بناءً على الكود السعودي.")

# --- العنوان الرئيسي ---
st.title("🏗️ نظام التدقيق الآلي لرخص البناء - الرياض")
st.markdown("---")

# --- تعليمات النظام (هنا سنضع عقلك الرقمي) ---
# ملاحظة هامة: هذا هو المكان الذي تضع فيه التعليمات التي كتبتها في AI Studio
# لقد وضعت لك نصاً افتراضياً، لكن يفضل أن تضع نصك الخاص ليكون دقيقاً
system_instruction = """
أنت مهندس مدقق خبير في إدارة رخص البناء بأمانة منطقة الرياض.
مرجعك الوحيد والحصري هو ملف 'الدليل الموحد لاشتراطات رخص البناء'.
مهمتك: عند إرفاق مخطط معماري، قم بتنفيذ التالي:
1. تحليل المخطط وتحديد نوعه (سكتي، تجاري، إلخ).
2. استخراج المخالفات الواضحة بناء على الكود العمراني لمدينة الرياض.
3. ذكر أرقام الصفحات أو البنود من الدليل الموحد إن أمكن.
4. كتابة التقرير بلهجة مهنية ورسمية.
"""

# --- منطق التطبيق ---
if api_key:
    # إعداد الموديل
    genai.configure(api_key=api_key)
    
    # واجهة رفع الملف
    uploaded_file = st.file_uploader("قم برفع صورة المخطط (JPG, PNG) أو ملف PDF", type=["jpg", "jpeg", "png"])

    if uploaded_file is not None:
        # عرض الصورة للمستخدم
        image = Image.open(uploaded_file)
        st.image(image, caption="المخطط المرفق", use_column_width=True)

        # زر التحليل
        if st.button("بدء التدقيق الآلي 🚀"):
            with st.spinner('جاري تحليل المخطط ومطابقته مع الاشتراطات... يرجى الانتظار'):
                try:
                    # استدعاء Gemini 1.5 Pro
                    model = genai.GenerativeModel('gemini-1.5-pro-latest', system_instruction=system_instruction)
                    response = model.generate_content(["قم بتدقيق هذا المخطط وإعطائي تقرير مفصل بالملاحظات والمخالفات إن وجدت.", image])
                    
                    # عرض النتيجة
                    st.success("تم التدقيق بنجاح!")
                    st.markdown("### 📋 تقرير التدقيق:")
                    st.markdown(response.text)
                    
                except Exception as e:
                    st.error(f"حدث خطأ أثناء المعالجة: {e}")
else:
    st.warning("الرجاء إدخال مفتاح API في القائمة الجانبية للبدء.")

# --- تذييل الصفحة ---
st.markdown("---")
st.caption("تم تطوير هذا النظام بواسطة المعماري سلطان | نسخة تجريبية")
